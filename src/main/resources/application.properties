

# JWT Secret key for signing the tokens 
jwt.secret=frtuIbW/7tBCgNocIIcC7iQ65f2CwBamkLmto4hoSnw=

# MySQL Database properties 
spring.datasource.url=jdbc:mysql://localhost:3306/camelq
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=Alv@Snooby@2104

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# JPA Configuration - optimize for memory usage
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Disable Swagger UI when in production
springfox.documentation.enabled=${SWAGGER_ENABLED:false}
spring.mvc.pathmatch.matching-strategy=ant-path-matcher

# Memory optimization for JPA/Hibernate
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Server configuration
server.address=0.0.0.0
server.port=${PORT:8080}
server.tomcat.max-threads=30
server.tomcat.min-spare-threads=5

# Redis configuration - disable if not used
spring.data.redis.repositories.enabled=false

